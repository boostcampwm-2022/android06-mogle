name: Android CI

on:
  push:
    branches: [ "main" ]
    
jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v3
    - name: set up JDK 11
      uses: actions/setup-java@v3
      with:
        java-version: '11'
        distribution: 'temurin'
        cache: gradle
        
    - name: Setup Firebase CLI
      uses: pocket-apps/action-setup-firebase@v2
      with:
        # Firebase token you can get by running `firebase login:ci`
        firebase-token: ${{secrets.FIREBASE_TOKEN}}
        
    - name: Create google-services.json
      run: echo '${{ secrets.GOOGLE_SERVICES_JSON }}' > ./app/google-services.json

    - name: Grant execute permission for gradlew
      run: chmod +x gradlew
      
    - name: build release 
      run: ./gradlew bundleRelease
          
    - name: upload debug artifact to Firebase App Distribution
      run: firebase appdistribution:distribute app/build/outputs/bundle/release/app-release.aab --token ${{secrets.FIREBASE_TOKEN}} --app ${{secrets.FIREBASE_APP_ID}} --testers "kbg0412@gmail.com" --release-notes "Test Distribution"
    
    - name: action-slack
      uses: 8398a7/action-slack@v3.14.0
      with:
        status: ${{ job.status }}
        fields: repo,message,commit,author,action,eventName,ref,workflow,job,took
      env:
         SLACK_WEBHOOK_URL: ${{ secrets.SLACK_WEBHOOK_URL }}
      if: always()
